spring:
  application:
    name: discodeit

  profiles:
    active: dev  # 기본은 dev, 운영 시 prod로 교체

  jpa:
    generate-ddl: false
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
        use_sql_comments: true
    open-in-view: false

discodeit:
  storage:
    type: local
    local:
      root-path: ./.storage

logging:
  level:
    root: info
    com.sprint.mission.discodeit: info
    org.hibernate.SQL: off  # debug가 default
    org.hibernate.orm.jdbc.bind: off # trace가 default

server:
  port: 8080
